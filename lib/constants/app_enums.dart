enum enumLoginType { normal, google, facebook, apple }

enum enumAppLanguage { english, arabic }

enum enumAddressType { shipping, billing }

enum enumOrderStatus { Pending, Shipped, Delivered }

enum EnumCouponType { monthlyDiscount, autoDiscount, userCoupon }
